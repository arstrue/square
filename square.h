#ifndef SQUARE_SQUARE_H
#define SQUARE_SQUARE_H

#endif //SQUARE_SQUARE_H

#include <stdio.h>
#include <math.h>
#include <assert.h>
#include <stdlib.h>
#define  _CRT_SECURE_NO_WARNINGS
#define epsilon 1e-7///<const for comparing double
#define a_ *(p + i*sizeof(double)*3 + 0*sizeof(double))
#define b_ *(p + i*sizeof(double)*3 + 1*sizeof(double))
#define c_ *(p + i*sizeof(double)*3 + 2*sizeof(double))
